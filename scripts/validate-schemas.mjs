import path from "node:path";import {fileURLToPath} from "node:url";import {args,need,readJson,writeJson} from "./lib/common.mjs";
const a=args(),work=path.resolve(need(a,"work")),root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."),jobs=[["content-inventory.json","content-inventory.schema.json"],["deck-plan.json","deck-plan.schema.json"]],issues=[];
function check(value,schema,loc){if(!schema||typeof schema!=="object")return;if(schema.type==="object"){if(value===null||typeof value!=="object"||Array.isArray(value)){issues.push({severity:"critical",code:"SCHEMA_TYPE",location:loc,expected:"object"});return;}for(const key of schema.required||[])if(!(key in value))issues.push({severity:"critical",code:"SCHEMA_REQUIRED",location:`${loc}.${key}`});for(const [key,child] of Object.entries(schema.properties||{}))if(key in value)check(value[key],child,`${loc}.${key}`);}else if(schema.type==="array"){if(!Array.isArray(value)){issues.push({severity:"critical",code:"SCHEMA_TYPE",location:loc,expected:"array"});return;}if(schema.minItems&&value.length<schema.minItems)issues.push({severity:"critical",code:"SCHEMA_MIN_ITEMS",location:loc,minimum:schema.minItems});value.forEach((v,i)=>check(v,schema.items,`${loc}[${i}]`));}else if(schema.type==="string"&&typeof value!=="string")issues.push({severity:"critical",code:"SCHEMA_TYPE",location:loc,expected:"string"});else if(schema.type==="integer"&&!Number.isInteger(value))issues.push({severity:"critical",code:"SCHEMA_TYPE",location:loc,expected:"integer"});else if(schema.type==="boolean"&&typeof value!=="boolean")issues.push({severity:"critical",code:"SCHEMA_TYPE",location:loc,expected:"boolean"});if(schema.enum&&!schema.enum.includes(value))issues.push({severity:"critical",code:"SCHEMA_ENUM",location:loc,value});if(schema.pattern&&typeof value==="string"&&!new RegExp(schema.pattern).test(value))issues.push({severity:"critical",code:"SCHEMA_PATTERN",location:loc,value});if(schema.minimum!==undefined&&typeof value==="number"&&value<schema.minimum)issues.push({severity:"critical",code:"SCHEMA_MINIMUM",location:loc,value});if(schema.minLength&&typeof value==="string"&&value.length<schema.minLength)issues.push({severity:"critical",code:"SCHEMA_MIN_LENGTH",location:loc});}
for(const [dataName,schemaName] of jobs){try{check(await readJson(path.join(work,dataName)),await readJson(path.join(root,"schemas",schemaName)),dataName);}catch(error){issues.push({severity:"critical",code:"SCHEMA_FILE_ERROR",file:dataName,message:error.message});}}
const report={category:"schemas",status:issues.length?"fail":"pass",issues};await writeJson(path.join(work,"qa-schemas.json"),report);console.log(`schemas=${jobs.length} issues=${issues.length}`);if(issues.length)process.exitCode=2;
