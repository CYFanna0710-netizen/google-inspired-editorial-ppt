import fs from "node:fs/promises";import path from "node:path";import {FileBlob,PresentationFile} from "@oai/artifact-tool";import {args,need} from "./lib/common.mjs";
const a=args(),input=need(a,"input"),out=need(a,"out");await fs.mkdir(out,{recursive:true});const deck=await PresentationFile.importPptx(await FileBlob.load(input));let i=0;for(const slide of deck.slides.items){i++;const png=await deck.export({slide,format:"png",scale:1});await fs.writeFile(path.join(out,`slide-${i}.png`),new Uint8Array(await png.arrayBuffer()));const layout=await slide.export({format:"layout"});await fs.writeFile(path.join(out,`slide-${i}.layout.json`),await layout.text());}const montage=await deck.export({format:"webp",montage:true,scale:1});await fs.writeFile(path.join(out,"montage.webp"),new Uint8Array(await montage.arrayBuffer()));console.log(`rendered=${i} out=${out}`);
